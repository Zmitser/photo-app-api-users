server:
  port: 0
spring:
  application:
    name: users-ws
  devtools:
    restart:
      enabled: true
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8010/eureka
gateway:
  ip-address: 192.168.100.2
